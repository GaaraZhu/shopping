{
    "name": "shopping",
    "description": "computer store checkout system",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "npm-run-all clean compile",
        "clean": "rimraf dist",
        "compile": "tsc --project tsconfig.json",
        "test": "jest --forceExit --detectOpenHandles --coverage"
    },
    "devDependencies": {
        "@types/jest": "^26.0.24",
        "jest": "^26.6.3",
        "jest-junit": "^12.0.0",
        "rimraf": "^3.0.2",
        "ts-jest": "^26.4.4",
        "typescript": "^4.1.2",
        "npm-run-all": "^4.1.5"
    }
}
